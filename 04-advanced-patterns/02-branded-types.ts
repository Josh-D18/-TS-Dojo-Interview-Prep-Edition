// 🏷️ Branded Types — protecting your domain logic from silent type bugs.

// 1️⃣ Create a type `UserId` that is a branded string (i.e. not interchangeable with other strings).
// Use a unique symbol brand.

// 2️⃣ Make a function `createUserId()` that returns a UserId safely.

// 3️⃣ Create two branded types: `Celsius` and `Fahrenheit`.
// Write a converter function that only accepts the correct branded type.

// 4️⃣ Bonus: Model a `PositiveNumber` branded type that disallows negatives at compile time.

// 💬 Reflection: Branded types are the “domain armor” of your app — they prevent cross-type pollution.
