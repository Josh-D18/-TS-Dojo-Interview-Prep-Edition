// 🦄 Singleton Pattern — one instance, typed and trusted.

// 1️⃣ Create a generic Singleton<T> pattern where:
// - You can define the class type to wrap
// - getInstance() always returns the same instance
// - Works for multiple types of classes (Database, Logger, etc.)

// 2️⃣ Add type safety so that only *one instance per type* is allowed.

// 3️⃣ Add a constraint: make sure the class has a default constructor.

// 4️⃣ Bonus: create a SingletonRegistry that tracks instances by class name.

// 💬 Thought exercise: when might singletons become a *type smell*?
