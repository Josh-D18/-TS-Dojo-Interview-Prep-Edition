// 🧠 Inference Flow — teaching your brain to read type logic like a compiler.

// 1️⃣ Create a type ExtractPromise<T> that unwraps a Promise’s resolved value type.

// 2️⃣ Build a Flatten<T> type that recursively removes array nesting (T[][][] -> T).

// 3️⃣ Create a DeepReadonly<T> type using recursion + inference.

// 4️⃣ Write a ConditionalInfer type that extracts argument types from a function signature.

// 5️⃣ Infer a discriminated union’s member type from a given tag:
// Example: ExtractMember<'success', ResultUnion>

// ⚔️ Meta Challenge: Create an "Infer Pipeline" — a type that passes the inferred result of one conditional into another.
// (Think: type flow composition.)

// 💬 Interview gem: "When should you prefer inference over explicit generics?"
