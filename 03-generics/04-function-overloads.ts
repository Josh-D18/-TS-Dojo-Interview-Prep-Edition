// ⚡ Function overloads — where inference meets chaos.

// 1️⃣ Create an overloaded function `formatInput` that:
// - returns number if passed a number
// - returns uppercase string if passed a string

// 2️⃣ Create a `getUser` overload that:
// - accepts an id (number) and returns a single user
// - accepts an array of ids and returns an array of users

// 3️⃣ Build a `combine` overload that merges arrays or concatenates strings.

// ⚔️ Interview exercise: How would you handle overlapping overload signatures cleanly?
