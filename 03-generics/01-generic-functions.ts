// 🧩 Goal: Master reusable function logic that adapts to types dynamically.

// 1️⃣ Basic identity challenge
// Write a function that returns whatever it receives — but *preserves* the type.

// 2️⃣ Swap function
// Build a function that swaps the positions of two items in a tuple — with type inference.

// 3️⃣ Array element extractor
// Build a function that takes an array and returns its first element, fully typed.

// 4️⃣ MapKeys challenge
// Create a function that takes an object and returns an array of its keys (typed, not just string[]).

// 5️⃣ Constraint twist
// Make a function that only works on arrays with items that have an `id: string` property.

// ❓Interview follow-up thought: how would you *narrow* generic inference when TypeScript infers too broadly?
