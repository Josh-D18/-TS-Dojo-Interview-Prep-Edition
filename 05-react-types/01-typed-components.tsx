// ⚡ Typed Components — mastering props like a pro.

// 1️⃣ Create a typed <Button> component that:
// - Requires a `label: string` prop
// - Accepts an optional `onClick` (with correct typing for events)
// - Accepts a generic `variant` prop: 'primary' | 'secondary' | 'ghost'

// 2️⃣ Create a <List> component using generics:
// - Takes items: T[]
// - Takes renderItem: (item: T) => ReactNode
// - The key? Type safety between item and renderItem.

// 3️⃣ Add discriminated prop unions:
// - Example: <Avatar> with either `src` OR `initials`, not both.

// ⚔️ Bonus: Create a polymorphic “as” component — <Text as="p" | "span" | "h1">
// Type challenge: preserve native element props dynamically.
